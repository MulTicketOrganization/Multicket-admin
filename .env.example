# 서버 사이드에서만 접근하는 백엔드 베이스 URL (httpOnly 쿠키 기반 프록시용)
# NEXT_PUBLIC_ 접두사 없음 → 브라우저 번들에 노출되지 않음
BACKEND_API_BASE_URL=http://localhost:8080

# 토큰을 담을 httpOnly 쿠키 이름
AUTH_COOKIE_NAME=mc_admin_token

# 쿠키 보안 옵션 (prod 에서는 true 로 두기)
# COOKIE_SECURE=true
